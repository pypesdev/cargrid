// Phase 0 placeholder: no ingesters yet (Phase 2). This is a no-op so the
// script wiring (`pnpm ingest:seed`) exists and is exercisable.
process.stdout.write("seed: no ingesters wired yet (Phase 2)\n");
